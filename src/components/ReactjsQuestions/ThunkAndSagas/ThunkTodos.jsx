import React from 'react'
import { useSelector } from 'react-redux'

export const ThunkTodos = () => {
  // const {todos, loading, error} = useSelector((state)=> state.todos)

  // console.log("todo", todos )

  return (
    <div>
      <h4>ThunkTodos</h4>
    </div>
  )
}
